# Sonic Visions

Mobile-first live audio visualizer for piano or microphone input. It listens through the browser mic, estimates note/rhythm/energy, and renders selectable generative visual worlds.

## Run

```bash
npm install
npm run dev
```

Open on a phone over HTTPS in production, then tap **Mic**.
